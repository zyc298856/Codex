# 论文审查意见修复记录

日期：2026-04-29

## 背景

根据 Claude 对仓库和论文的二次审查报告，论文整体与代码实现保持一致，没有发现明显虚构功能或运行级错误。本次修改重点处理审查报告中建议的低风险、高收益文本完善项，避免影响已经稳定的论文排版和项目代码。

## 已采纳修改

1. 摘要补充软件报警输出。
   - 中文摘要补充“画面报警提示和事件日志记录”。
   - 英文摘要补充 “visual alarm overlay and event logging”。

2. 第二章增强技术背景。
   - 2.1 节补充 YOLO 多尺度检测头、YOLOv10 NMS-free 思路以及 `end2end=False` 对 RKNN 后处理部署的意义。
   - 2.4 节补充 RKNN Runtime 调用链，包括 `rknn_init`、`rknn_inputs_set`、`rknn_set_io_mem`、`rknn_run`、`rknn_outputs_get`。
   - 2.5 节补充 C++17 多线程相关机制，包括 `std::thread`、`std::mutex`、`std::condition_variable`、`std::atomic`、有界队列和多 context worker。

3. 轻量跟踪描述进一步具体化。
   - 3.5.3 节和 4.6 节将运动跟踪描述为基于连续检测框中心点位移向量的线性运动预测。
   - 光流跟踪补充 Lucas-Kanade 稀疏光流和 Shi-Tomasi 特征点检测依据。
   - 明确本文跟踪策略不同于 DeepSORT 等重型外观特征关联方法。

4. INT8、RGA、GPIO 相关表述保持真实。
   - 第 6 章继续明确：INT8 RKNN 已完成离线转换，但仍需上板做精度、延迟和稳定性对比，不写成最终优于 FP 主路径的结论。
   - RGA 相关表述限定为已实现和部分验证的优化路径，不夸大为最终主路径。
   - GPIO 外设报警说明为软件画面报警和事件日志替代方案，避免声称已有继电器或蜂鸣器硬件闭环。

5. 参考文献扩展。
   - 新增 Lucas-Kanade 光流、Shi-Tomasi 特征点、量化推理、MobileNetV2、DeepSORT 和 C++17 标准相关文献。
   - 参考文献编号扩展到 `[20]`，与新增正文引用保持一致。

## 未修改或无需修改项

1. 审查报告提到表格中可能存在 `0.00 ms` 复用帧显示问题。实际检查当前正式论文未发现 `0.00` 或 `0.00 ms` 残留，因此未做额外替换。
2. 审查报告提到 `C/C++` 表述问题。当前正式论文未发现 `C/C++` 残留，正文已使用 `C++17` 或 `C++` 表述。
3. 本轮未修改项目代码，只修正文档和论文文字。

## 版式验证

1. 使用 Microsoft Word COM 更新目录和域。
2. 导出 PDF 进行版式检查。
3. 将 PDF 渲染为 58 页 PNG。
4. 抽查封面、声明页、摘要、目录、第二章新增内容、第三章轻量跟踪段落、第四章实时 RTSP 与跟踪段落、第六章总结与展望、参考文献和致谢页面。
5. 未发现图片遮挡、题注错位、表格压线或参考文献格式明显异常。

## 当前结论

当前 `paper/full_thesis_latest_merged.docx` 可作为审查意见处理后的最新版正式论文继续使用。后续如果继续补充实验数据，应优先追加到第五章实验记录和第六章展望，不建议大幅重排前文结构。
